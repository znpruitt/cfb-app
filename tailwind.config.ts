// tailwind.config.ts
import type { Config } from 'tailwindcss';

const config: Config = {
  // No `content` in v4
  darkMode: 'media',
  theme: {
    extend: {},
  },
  plugins: [],
};

export default config;
